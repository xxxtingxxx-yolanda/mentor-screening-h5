# 导师筛选 H5 - 开发 To Do（给另一位开发）

**一、数据准备**
- 从 `导师助手/湖南工业大学包装艺术设计学院研究生导师基本信息.docx` 提取导师信息。
- 统一字段结构，清洗研究方向与备注中的长文本。
- 生成 `mentors.json`（建议放在 `app` 或 `web` 静态目录）。
- 按 `导师助手/导师照片/<方向>/<职称>/<姓名>.jpg` 规则关联图片。
- 为缺失数据字段设定占位文案或默认值。

**二、前端基础结构**
- 建一个静态 H5 目录结构（如 `web/` 或 `app/src/main/assets/`）。
- 拆分页面与组件：
  - 首页/说明页
  - 筛选区
  - 列表区
  - 详情弹层或详情页
  - 候选清单

**三、核心功能**
- 实现方向筛选（必选）。
- 实现职称与关键词筛选（可多选）。
- 实现导师列表渲染与排序。
- 实现详情查看（支持返回保留筛选状态）。
- 实现候选清单的加入/移除与本地持久化（localStorage）。
- 实现一键复制（邮箱/候选清单文本）。

**四、体验与适配**
- 移动端优先布局，适配主流手机尺寸。
- 长文本折叠与展开（备注过长时）。
- 图片缺失时的默认头像与占位。
- 处理 iOS/Android 微信内置浏览器的兼容性。

**五、QA 与验证**
- 在 Android 模拟器中验证 H5 访问与交互流程。
- 校验筛选结果数量与预期一致。
- 检查所有导师图片与资料卡片是否能正常显示。
- 检查复制功能在不同浏览器下可用性。

**六、交付**
- 输出可直接部署的静态文件包。
- 简要说明如何更新 `mentors.json` 与图片目录。
