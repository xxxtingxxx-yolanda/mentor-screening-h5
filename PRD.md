# 导师筛选 H5 网站 PRD

版本：v0.2
日期：2026-02-18

## 一、项目背景
- 学生在选择导师时，常见问题是信息分散、匹配效率低、联系路径不清晰。
- 目标不是做“信息堆砌网站”，而是做“可决策的筛选流程”：先了解学生，再给出更精准的导师推荐与联系入口。

## 二、产品目标
- 让学生在 3 分钟内完成信息填写并拿到导师建议列表。
- 基于学生填写内容进行后台隐形打分，提升推荐准确度。
- 对高匹配学生开放重点导师直投通道（田飞老师、何铭锋老师），缩短沟通链路。
- 其他导师保留邮箱联系模式，保证信息覆盖完整。

## 三、产品原则（以田老师思路为主）
- H5 就是网站：轻量、直接、路径清晰，先解决核心问题。
- 手机优先：流程必须在手机上顺畅完成，PC 作为兼容端。
- 先实用再复杂：先做可运行的评分和分流，再做算法精细化。
- 推荐要克制：不展示打分细节给学生，避免“分数导向焦虑”。

## 四、用户与场景
- 目标用户：报考或在读研究生（设计相关方向为主）。
- 核心场景：
  - 学生不确定选谁，先填方向、技能、职业规划，系统给出推荐。
  - 高匹配学生希望快速触达目标导师，直接提交简历。
  - 普通浏览用户只想看导师信息并通过邮箱联系。

## 五、核心流程
1. 学生进入 H5，阅读说明并开始填写。
2. 填写学生画像表单：
   - 研究方向（自由填写）
   - 已掌握技能（自由填写，可多项）
   - 未来职业规划（自由填写）
3. 系统后台完成隐形评分（学生不可见）。
4. 系统返回导师推荐结果：
   - 若达到定向阈值，优先推送田飞老师、何铭锋老师。
   - 未达到阈值，返回常规导师列表（按匹配顺序）。
5. 联系方式分流：
   - 田飞老师、何铭锋老师：提供“简历直投”入口。
   - 其他导师：仅展示联系邮箱。

## 六、功能需求
### F1 学生画像填写
- 必填字段：
  - `target_direction`（目标方向）
  - `current_skills`（已掌握技能）
  - `career_plan`（未来职业规划）
- 字段长度限制：
  - 目标方向：10-200 字
  - 已掌握技能：10-500 字
  - 职业规划：20-1000 字

### F2 后台隐形评分
- 系统基于学生画像与导师标签进行评分。
- 分数仅用于系统推荐与分流，前端不展示数值、不展示规则细节。
- 需要记录评分日志（用于后续优化），日志不对学生公开。

### F3 推荐与分流
- 推荐结果至少返回 3 位导师。
- 当学生满足“重点导师阈值”时：
  - 结果页置顶显示田飞老师、何铭锋老师卡片。
  - 卡片展示“可直投简历”按钮。
- 未满足阈值时：
  - 正常展示导师列表，仅保留邮箱联系。

### F4 导师信息展示
- 导师卡片字段：
  - 头像、姓名、职称、研究方向关键词、联系方式类型（直投/邮箱）。
- 导师详情字段：
  - 研究方向、籍贯、出生年份、邮箱、备注、成果简介。
- 缺失数据统一展示“未公开/待补充”。

### F5 简历直投（仅指定导师）
- 仅对田飞老师、何铭锋老师开放。
- 学生提交内容：
  - 姓名
  - 手机号
  - 邮箱
  - 简短自述（可选）
  - 简历附件（PDF/DOC/DOCX，最大 10MB）
- 提交成功后提示“已提交成功，请等待导师团队联系”。
- 后台将简历投递到对应导师接收通道（邮箱/API/管理端）。

### F6 邮箱联系（其他导师）
- 导师详情页提供复制邮箱按钮。
- 不开放附件上传，避免流程复杂化。

### F7 风险提示与声明
- 页面显著位置展示：
  - 信息来源说明
  - 打分为辅助分流，不代表录取结论
  - 建议以学院官方信息为最终准确信息

## 七、打分逻辑（MVP 规则版）
- 评分维度（建议）：
  - 方向匹配度：50%
  - 技能匹配度：30%
  - 职业规划匹配度：20%
- 结果用途：
  - `score >= threshold_direct`：进入重点导师优先推荐池。
  - `score < threshold_direct`：进入常规推荐池。
- 阈值先以配置项管理，支持后续运营调整。

## 八、信息架构
- `页面1：首页/说明`
- `页面2：学生画像填写`
- `页面3：推荐结果列表`
- `页面4：导师详情`
- `页面5：简历直投提交页（仅定向入口可见）`

## 九、数据结构建议
### 1) 导师数据 `mentors.json`
- `id`
- `name`
- `direction`
- `title`
- `research_areas` (array)
- `skills_tags` (array)
- `career_tags` (array)
- `email`
- `photo_path`
- `allow_direct_apply` (boolean)
- `notes`

### 2) 学生提交数据 `student_profile`
- `session_id`
- `target_direction`
- `current_skills`
- `career_plan`
- `created_at`

### 3) 评分结果 `match_result`
- `session_id`
- `mentor_id`
- `score`
- `is_direct_channel`
- `created_at`

### 4) 直投记录 `direct_apply`
- `session_id`
- `mentor_id`
- `student_name`
- `student_phone`
- `student_email`
- `intro`
- `resume_file_url`
- `status`（submitted/forwarded/failed）
- `created_at`

## 十、技术方案
- 前端：H5（HTML/CSS/JS 或 Vue/React 均可），移动端优先。
- 后端：轻量接口服务（用于评分与简历上传）。
- 存储：
  - 导师静态数据：JSON
  - 简历文件：对象存储或安全文件目录
  - 提交与评分日志：数据库
- 安全：
  - 上传文件类型校验 + 大小校验
  - 手机号/邮箱格式校验
  - 接口限流与基础防刷

## 十一、权限与隐私
- 学生首次提交前需勾选隐私授权：
  - 同意用于导师匹配与投递处理。
- 简历和联系方式仅用于本项目投递，不对外展示。
- 提供数据删除入口（可邮箱申请删除）。

## 十二、验收标准（MVP）
- 学生可完整提交方向、技能、职业规划并拿到推荐结果。
- 评分逻辑生效且前端不展示任何分数。
- 高分用户可看到田飞老师、何铭锋老师直投入口并完成上传。
- 其他导师仅显示邮箱联系，不出现直投按钮。
- 关键流程在主流手机浏览器可用（微信内置浏览器 + Chrome）。

## 十三、已知风险与待确认
- 人名确认：当前按资料库使用“何铭锋老师”；如最终名单为“何敏峰/方明峰”，需在数据配置统一替换。
- 打分规则初期为规则引擎，后续可升级为模型评分。
- 简历直投涉及个人信息，需尽快确定最终接收方式（邮箱/API/管理后台）。

## 十四、里程碑建议
1. M1（1-2天）：数据清洗 + 导师标签化 + 评分规则配置化。
2. M2（2-3天）：H5 填写页 + 推荐页 + 详情页打通。
3. M3（1-2天）：直投上传链路 + 后台转发。
4. M4（1天）：移动端兼容、文案优化、验收测试。
