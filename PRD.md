# 导师筛选 H5 网站 PRD

版本：v0.2
日期：2026-02-18

## 一、项目背景
- 学生在选择导师时，常见问题是信息分散、匹配效率低、联系路径不清晰。
- 目标不是做“信息堆砌网站”，而是做“可决策的筛选流程”：先了解学生，再给出更精准的导师推荐与联系入口。

## 二、产品目标
- 让学生在 3 分钟内完成信息填写并拿到导师建议列表。
- 基于学生填写内容进行后台隐形打分，提升推荐准确度。
- 高匹配用户优先推荐田飞老师、何铭锋老师（仅排序优先，不展示打分细节）。
- 全部导师统一使用邮箱联系模式，保证流程简洁稳定。

## 三、产品原则（以田老师思路为主）
- H5 就是网站：轻量、直接、路径清晰，先解决核心问题。
- 手机优先：流程必须在手机上顺畅完成，PC 作为兼容端。
- 先实用再复杂：先做可运行的评分和分流，再做算法精细化。
- 推荐要克制：不展示打分细节给学生，避免“分数导向焦虑”。

## 四、用户与场景
- 目标用户：报考或在读研究生（设计相关方向为主）。
- 核心场景：
  - 学生不确定选谁，先选方向、技能、职业规划标签，系统给出推荐。
  - 普通浏览用户只想看导师信息并通过邮箱联系。

## 五、核心流程
1. 学生进入 H5，阅读说明并开始填写。
2. 填写学生画像表单：
   - 目标方向（多选标签，支持少量补充）
   - 已掌握技能（多选标签，支持少量补充）
   - 未来职业规划（多选标签，支持少量补充）
3. 系统后台完成隐形评分（学生不可见）。
4. 系统返回导师推荐结果：
   - 若达到定向阈值，优先推送田飞老师、何铭锋老师。
   - 未达到阈值，返回常规导师列表（按匹配顺序）。
5. 联系方式分流：
   - 所有导师仅展示联系邮箱（复制邮箱）。

## 六、功能需求
### F1 学生画像填写
- 必填字段：
  - `target_direction`（目标方向）
  - `current_skills`（已掌握技能）
  - `career_plan`（未来职业规划）
- 交互方式：
  - 三个字段均采用“多选标签”为主，减少输入成本。
  - 支持“补充说明（可选）”文本输入。
  - 校验规则为“至少选择 1 项”，不做强制字数限制。

### F2 后台隐形评分
- 系统基于学生画像与导师标签进行评分。
- 分数仅用于系统推荐与分流，前端不展示数值、不展示规则细节。
- 需要记录评分日志（用于后续优化），日志不对学生公开。

### F3 推荐与分流
- 推荐结果至少返回 3 位导师。
- 当学生满足“重点导师阈值”时：
  - 结果页置顶显示田飞老师、何铭锋老师卡片。
- 未满足阈值时：
  - 正常展示导师列表，仅保留邮箱联系。

### F4 导师信息展示
- 导师卡片字段：
  - 头像、姓名、职称、研究方向关键词、联系方式（邮箱复制）。
- 导师详情字段：
  - 研究方向、籍贯、出生年份、邮箱、备注、官网详情链接。
- 缺失数据统一展示“未公开/待补充”。

### F5 邮箱联系（全部导师）
- 导师详情页提供复制邮箱按钮。
- 不开放附件上传，保持流程简单、稳定、轻维护。

### F6 风险提示与声明
- 页面显著位置展示：
  - 信息来源说明
  - 打分为辅助分流，不代表录取结论
  - 建议以学院官方信息为最终准确信息

## 七、打分逻辑（MVP 规则版）
- 评分维度（建议）：
  - 方向匹配度：50%
  - 技能匹配度：30%
  - 职业规划匹配度：20%
- 结果用途：
  - `score >= threshold_direct`：进入重点导师优先推荐池。
  - `score < threshold_direct`：进入常规推荐池。
- 阈值先以配置项管理，支持后续运营调整。

## 八、信息架构
- `页面1：首页/说明`
- `页面2：学生画像填写`
- `页面3：推荐结果列表`
- `页面4：导师详情`
- `页面5：简历直投提交页（仅定向入口可见）`

## 九、数据结构建议
### 1) 导师数据 `mentors.json`
- `id`
- `name`
- `direction`
- `title`
- `research_areas` (array)
- `skills_tags` (array)
- `career_tags` (array)
- `email`
- `photo_path`
- `profile_url` (string)
- `official_photo_url` (string)
- `notes`

### 2) 学生提交数据 `student_profile`
- `session_id`
- `target_direction`
- `current_skills`
- `career_plan`
- `created_at`

### 3) 评分结果 `match_result`
- `session_id`
- `mentor_id`
- `score`
- `created_at`

## 十、技术方案
- 前端：H5（HTML/CSS/JS 或 Vue/React 均可），移动端优先。
- 后端：轻量接口服务（用于评分日志与数据更新任务）。
- 存储：
  - 导师静态数据：JSON
  - 提交与评分日志：数据库
- 安全：
  - 输入内容基础校验
  - 接口限流与基础防刷

## 十一、权限与隐私
- 学生首次提交前需勾选隐私授权：
  - 同意用于导师匹配处理。
- 联系方式仅用于学生主动联系导师，不对外批量展示。
- 提供数据删除入口（可邮箱申请删除）。

## 十二、验收标准（MVP）
- 学生可完整提交方向、技能、职业规划并拿到推荐结果。
- 评分逻辑生效且前端不展示任何分数。
- 产品设计方向或高匹配用户可看到田飞老师、何铭锋老师置顶推荐。
- 全部导师仅显示邮箱联系，不出现直投按钮。
- 关键流程在主流手机浏览器可用（微信内置浏览器 + Chrome）。

## 十三、已知风险与待确认
- 人名确认：当前按资料库使用“何铭锋老师”；如最终名单为“何敏峰/方明峰”，需在数据配置统一替换。
- 打分规则初期为规则引擎，后续可升级为模型评分。
- 官网导师页会动态更新，需定期同步导师信息（建议周级更新）。

## 十四、里程碑建议
1. M1（1-2天）：数据清洗 + 导师标签化 + 评分规则配置化。
2. M2（2-3天）：H5 填写页 + 推荐页 + 详情页打通。
3. M3（1-2天）：官网导师信息自动抓取更新脚本 + 数据校验。
4. M4（1天）：移动端兼容、文案优化、验收测试。
